import { StyleSheet, processColor } from 'react-native';
import invariant from 'ts-tiny-invariant';
export const defaultStyle = StyleSheet.create({
    container: {
        backgroundColor: '#fff0'
    }
});
export const checkStyle = (style) => {
    if (style) {
        const { backgroundColor } = StyleSheet.flatten(style);
        invariant(processColor(backgroundColor) !== 0, `ImageFilter: Can't use '${String(backgroundColor)}' backgroundColor,` +
            " consider using '#fff0' instead.");
    }
};
//# sourceMappingURL=style.js.map